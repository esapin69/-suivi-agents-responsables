{
  "name": "ghe-suivi-agents-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types --check && tsc --noEmit && wrangler deploy --dry-run",
    "types": "wrangler types",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "4.120.0"
  }
}
